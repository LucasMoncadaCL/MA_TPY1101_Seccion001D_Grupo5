name: Docs Alignment

on:
  pull_request:

jobs:
  docs-alignment:
    name: Verify Documentation Alignment
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run docs alignment checks
        shell: pwsh
        run: ./Producto/backendpanol/scripts/docs/verify-doc-alignment.ps1 -RepoRoot .
